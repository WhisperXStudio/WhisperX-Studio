Execute the attached WHISPERX | STUDIO Final Master Lock Spec against the current official repository:



WhisperXStudio/WhisperX-Studio

Treat the complete specification as the governing implementation contract.

Do not summarize the specification.
Do not rewrite it.
Do not propose a different design.
Do not create a parallel concept.
Do not stop after repository analysis or documentation.

Begin by inspecting:

1. the complete current repository;
2. the active branch and working tree;
3. the current rendered application;
4. every existing route and major work surface;
5. existing design tokens and shared primitives;
6. existing JSON, schemas, registries, and consumers;
7. editor, preview, terminal, Git, Agent, skills, modules, motion, and deployment implementation;
8. current desktop, tablet, mobile, dark-mode, and light-mode behavior;
9. package configuration, lockfiles, scripts, build, runtime, and deployment configuration.

Create an internal verified inventory and classify all major capabilities using only:

IMPLEMENTED
CONNECTED
PARTIAL
SIMULATED
PLACEHOLDER
PLANNED
BLOCKED
UNSUPPORTED
DEPRECATED
REMOVED

Then execute the required sequence:

DISCOVER
→ INSPECT
→ INVENTORY
→ STABILIZE
→ PRESERVE
→ COMPLETE
→ CONNECT
→ EXPAND
→ VALIDATE
→ BUILD
→ REVIEW
→ DOCUMENT
→ SHIP

Preserve the approved rendered WHISPERX | STUDIO identity.

Complete all safely executable in-scope frontend work.

Do not replace approved production-quality implementation with generic components.

Do not stop after one route, one workspace, one polished screen, one successful build, or documentation generation.

Use representative or simulated frontend data only when real integration is unavailable, and label its status honestly.

Do not deploy to production without explicit authority.

At completion, provide the exact evidence required by Section 50, including:

- implementation summary;
- changed-files summary;
- route and module status matrix;
- capability status matrix;
- JSON consumption status;
- spatial-object registry status;
- actual validation commands and results;
- remaining partial, simulated, blocked, unsupported, or planned capabilities;
- verified blockers;
- regression confirmation;
- precise final completion statement.

Begin implementation now.

# WHISPERX | STUDIO — FINAL MASTER LOCK SPEC

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PART I — APPROVED CURRENT UX/UI: SOURCE OF TRUTH

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The current WHISPERX | STUDIO UX/UI direction is approved.

The existing rendered interface and current production-quality implementation are the visual, interaction, and product source of truth.

Do not interpret “production polish,” “system upgrade,” “full update,” “rewrite,” “production-ready,” or “refactor” as permission to redesign the product.

Do not replace the current design language with a new concept.

Do not simplify the approved interface into a generic developer dashboard, traditional IDE, SaaS application, enterprise control panel, component demo, VS Code clone, Cursor clone, or collection of disconnected tools.

The objective is to preserve the existing experience while completing, connecting, stabilizing, validating, and refining the system underneath it.

The approved product is not waiting for a new design concept.

It is waiting for completion.

---

## 1. CORE EXPERIENCE

WHISPERX | STUDIO is a premium AI-native creative development environment combining:

- editorial design;
- creative tooling;
- technical precision;
- cinematic composition;
- AI-assisted workflows;
- spatial workspace behavior;
- production-oriented development tools.

The experience must feel intentional, crafted, calm, precise, spatial, and alive.

Users must move naturally between:

IDEA → DESIGN → JSON → COMPONENT → CODE → AGENT → MOTION → PREVIEW → TERMINAL → GIT → VALIDATE → BUILD → DEPLOY → SHIP

These areas must feel like one product, not separate applications assembled inside one shell.

---

## 2. APPROVED VISUAL CHARACTER

The approved visual character is:

- editorial;
- asymmetrical;
- spatial;
- technical;
- minimal;
- premium;
- cinematic;
- architectural;
- controlled;
- expressive without visual noise.

Create deliberate contrast between:

- large and small;
- thin and bold;
- dense and open;
- dark and light;
- static and moving;
- technical and editorial;
- foreground and background;
- organic and computational.

Do not make every section symmetrical.

Do not center every composition.

Do not place every feature inside identical cards.

Do not give every panel equal radius, padding, height, spacing, or visual priority.

Hierarchy must come from scale, typography, position, negative space, overlap, depth, cropping, alignment, contrast, motion, opacity, and signal color.

---

## 3. APPROVED BRAND PRESENTATION

Official public lockup:

WHISPERX | STUDIO

Human-readable product name:

WhisperX Studio

Compact product identity:

WPX Studio

Compact system identity and prefix:

WPX

The X is the controlled visual signature.

It may function as:

- signature letter;
- active-state signal;
- loading mark;
- cursor response;
- transition mask;
- selection marker;
- divider;
- motion trigger;
- visual crop;
- system-status indicator;
- navigation threshold;
- execution signal.

Do not place red X graphics everywhere.

Use the X only when it strengthens recognition, hierarchy, interaction, transition, or system meaning.

---

## 4. APPROVED COLOR LANGUAGE

Preserve:

- deep layered dark surfaces;
- warm or neutral light surfaces;
- restrained greys;
- controlled border contrast;
- signal-red accents.

Red is a signal, not general decoration.

Use red for:

- the X;
- active navigation;
- focused commands;
- selected items;
- execution states;
- progress;
- warnings;
- validation;
- key motion cues;
- small visual anchors.

Do not use generic purple-blue AI gradients, rainbow gradients, uncontrolled glow, or large red background fields.

Dark mode must use multiple dark levels.

Light mode must use warm editorial and architectural surfaces, not a sterile white inversion.

---

## 5. APPROVED TYPOGRAPHY LANGUAGE

Typography is structural.

Preserve contrast between:

THIN × BOLD  
EDITORIAL × MONO  
LARGE × MICRO

Use:

- thin and light display typography for openness;
- bold or black emphasis for anchors and commands;
- mono typography for code, JSON, terminal, metadata, IDs, schemas, logs, and system state;
- cropped or offset large typography where purposeful;
- compact uppercase micro labels where appropriate.

Brand lockup:

WHISPER — thin or light  
X — stronger weight and signal color  
STUDIO — tracked, restrained, technical or mono

Do not convert everything into medium-weight sans-serif.

---

## 6. APPROVED LAYOUT LANGUAGE

Use an editorial spatial grid.

The implementation may use twelve columns internally, but the result must not resemble a generic template.

Approved structures include:

- uneven columns;
- wide and narrow regions;
- offset panels;
- large canvas areas;
- compact technical rails;
- floating controls;
- overlapping typography;
- asymmetrical splits;
- controlled negative space;
- contextual inspectors;
- variable-height regions.

Differentiate between:

page, section, workspace, canvas, region, rail, dock, inspector, panel, command surface, overlay, drawer, sheet, popover, and status bar.

A panel is not automatically a card.

A canvas is not a dashboard.

A section is not automatically a rounded container.

A dock is not automatically a sidebar.

---

## 7. APPROVED LANDING PAGE EXPERIENCE

The existing landing page is the visual DNA of the entire product.

Preserve:

- editorial hero composition;
- strong typography;
- clear Studio entrance;
- product previews inside the narrative;
- layered depth;
- controlled motion;
- asymmetrical sections;
- dark and light contrast;
- spacious composition;
- technical detail with visual refinement.

Do not replace it with a centered headline and two generic buttons.

Entering Studio must feel like entering the product introduced on the landing page.

---

## 8. APPROVED STUDIO WORKSPACE

The Studio is a spatial, multi-surface work environment.

It may include:

- top command bar;
- activity rail;
- navigation or explorer;
- editor or canvas;
- preview;
- contextual inspector;
- AI work surface;
- bottom output panel;
- status bar;
- command palette;
- overlays.

Priority:

1. Primary workspace — dominant.
2. Inspector — compact and contextual.
3. Navigation rail — small and persistent where useful.
4. Bottom panel — dense and collapsible.
5. AI workspace — integrated, not decorative.

Do not convert Studio into a dashboard of summary cards.

---

## 9. APPROVED EDITOR EXPERIENCE

The editor must feel native to WHISPERX.

A mature editor engine may be used, but it must be wrapped in WHISPERX visual language.

Preserve:

- thin structural borders;
- restrained tabs;
- clear active file;
- subtle dirty state;
- compact breadcrumbs;
- readable line numbers;
- controlled syntax colors;
- strong focus states;
- minimal toolbars;
- compact status;
- contextual AI actions;
- preview synchronization.

Code remains the visual focus.

---

## 10. APPROVED AI CHAT AND AGENT UX

AI Chat is a core work surface, not a support chatbot.

It may appear as:

- docked panel;
- contextual sheet;
- split workspace;
- selected-code assistant;
- selected-component assistant;
- command surface;
- task-execution timeline;
- inline action;
- review and approval surface.

Distinguish:

user request, execution summary, plan, proposed edit, command, tool result, diff, approval, progress, warning, error, and success.

Do not expose hidden chain-of-thought.

Show concise execution summaries and actionable results.

---

## 11. APPROVED JSON STUDIO

JSON Studio must connect:

- structured data;
- live preview;
- schema;
- validation;
- design tokens;
- components;
- layouts;
- motion;
- responsive behavior;
- runtime state.

It may include:

- JSON tree;
- code editor;
- visual preview;
- schema inspector;
- validation state;
- version state;
- migration state;
- source information;
- runtime diagnostics.

Do not present JSON Studio as a textarea inside a generic card.

Do not show fake success.

---

## 12. APPROVED MOTION LANGUAGE

Motion must be controlled, cinematic, technical, purposeful, interruptible, and reduced-motion aware.

Use:

- parallax;
- scroll-triggered transitions;
- subtle resistance moments;
- pinned sequences where justified;
- mask reveals;
- layered panel transitions;
- restrained blur;
- shared-layout transitions;
- responsive hover;
- velocity-aware behavior.

Motion scale:

- large for landing moments;
- medium for routes and panels;
- small for controls;
- minimal inside dense technical surfaces.

Avoid constant floating, bouncing, glowing, scroll hijacking, blocking animation, and layout instability.

---

## 13. APPROVED SURFACE AND DEPTH SYSTEM

Create depth through:

- layered surfaces;
- tonal contrast;
- subtle borders;
- controlled shadows;
- opacity;
- restrained blur;
- overlapping planes;
- scale;
- spatial separation;
- controlled lighting.

Avoid glass everywhere, heavy blur everywhere, large shadows, glossy surfaces, and floating every container.

---

## 14. APPROVED COMPONENT STYLE

Components must be contextual.

Preserve:

- restrained radius;
- sharp or lightly softened geometry;
- technical borders;
- compact controls;
- strong hierarchy;
- clear active states;
- minimal icons;
- small signal-red details;
- context-aware density.

Use pills only for compact status, filters, tags, selections, and metadata.

Use cards only for genuinely self-contained content.

---

## 15. APPROVED RESPONSIVE BEHAVIOR

Do not shrink desktop into mobile.

Desktop:

- multi-panel;
- keyboard-first;
- resizable;
- persistent context;
- parallel tools.

Tablet:

- fewer panels;
- contextual switching;
- split view where useful;
- touch-safe controls.

Mobile:

- focused single-panel workflows;
- bottom dock or contextual navigation;
- sheets and drawers;
- touch-first controls;
- simplified inspector;
- mobile AI Chat;
- preview-first behavior;
- progressive disclosure.

Preserve brand typography, red signals, hierarchy, motion identity, and focus.

---

## 16. APPROVED DARK MODE

Dark mode is immersive, focused, cinematic, deep, precise, calm, and premium.

Use multiple layers:

background, workspace, panel, elevated panel, overlay, selected, focused.

Avoid pure black everywhere.

Red remains restrained.

Syntax colors must not compete with brand red.

---

## 17. APPROVED LIGHT MODE

Light mode is an independent editorial experience.

Use:

- warm paper surfaces;
- off-white layers;
- subtle grey structure;
- sharp dark typography;
- controlled red signals;
- fine technical borders;
- soft architectural shadows.

Avoid sterile enterprise styling and excessive grey cards.

---

## 18. APPROVED HOVER AND INTERACTION SYSTEM

Use contextual hover behavior:

- fine-line extension;
- signal-red appearance;
- small translation;
- subtle scale;
- opacity shift;
- icon movement;
- background reveal;
- border activation;
- masked-label change.

Do not use the same hover on every element.

Focus states must remain visible.

Keyboard interaction must receive the same design quality as pointer interaction.

---

## 19. APPROVED SYSTEM STATES

All states must use the same visual language.

Required state families:

- loading;
- skeleton;
- empty;
- error;
- offline;
- success;
- permission denied;
- unsupported;
- partial;
- simulated;
- blocked.

States must be concise, actionable, honest, and connected to real behavior.

---

## 20. APPROVED CONTENT DENSITY

Landing:

open, cinematic, spacious, narrative.

Project pages:

balanced, informative, structured.

Studio:

dense but controlled.

Terminal, logs, problems:

high-density mono with minimal ornament.

AI and documentation:

readable, layered, scannable.

---

## 21. WHAT MUST BE PRESERVED

Preserve:

- current brand identity;
- WHISPERX | STUDIO naming;
- WPX compact language;
- X signature;
- red signal system;
- typography contrast;
- asymmetrical composition;
- landing-page DNA;
- spatial Studio concept;
- navigation direction;
- panel hierarchy;
- motion personality;
- dark mode;
- light mode;
- desktop composition;
- mobile direction;
- high-quality components;
- high-quality assets;
- useful interactions;
- useful motion;
- valid design tokens;
- JSON already driving the interface;
- user-authored work;
- production-quality implementation.

---

## 22. WHAT MAY BE IMPROVED

Improve only verified issues:

- inconsistent spacing;
- misalignment;
- clipping;
- overflow;
- weak contrast;
- inaccessible focus;
- missing keyboard behavior;
- unclear active states;
- broken responsiveness;
- incomplete mobile interactions;
- incomplete states;
- inconsistent tokens;
- duplicated styling;
- inefficient animation;
- weak loading;
- missing data;
- broken transitions;
- unclear technical status;
- unsupported claims;
- performance issues;
- hydration issues;
- build issues;
- runtime instability.

All improvements must look like refinements of the same product.

---

## 23. WHAT MUST NOT CHANGE

Do not:

- redesign the product;
- replace the landing page;
- replace navigation without verified evidence;
- replace typography;
- replace the red system;
- add generic AI gradients;
- add excessive cards, pills, glow, glass, gradients, or dashboard widgets;
- center every composition;
- equalize panels;
- remove asymmetry;
- remove editorial typography;
- remove the X identity;
- reduce Studio to file tree plus code editor;
- copy another IDE or SaaS product;
- create placeholder-quality routes;
- create disconnected modules;
- overwrite strong work with old assets;
- destroy user-authored work;
- remove useful functionality for visual simplicity.

---

## 24. IMPLEMENTATION DECISION RULE

When choosing between:

A. Easy implementation that weakens approved UX/UI.

B. Maintainable production implementation that preserves approved UX/UI.

Choose B.

For verified corrections:

1. identify the problem;
2. preserve original intention;
3. make the smallest effective correction;
4. validate dark mode;
5. validate light mode;
6. validate desktop;
7. validate tablet;
8. validate mobile;
9. validate keyboard use;
10. document significant deviations.

---

## 25. FINAL DESIGN PRESERVATION STATEMENT

The current WHISPERX | STUDIO UX/UI is approved.

It is not waiting for a new concept.

It is waiting for completion.

Do not redesign.

Complete it.

Refine it.

Stabilize it.

Validate it.

Polish it.

Ship it.

---

## 26. EXISTING IMPLEMENTATION INSPECTION — MANDATORY

Before modifying any route, component, layout, motion system, interaction, token, JSON structure, asset, or responsive behavior, inspect the existing implementation.

Inspect:

- repository;
- rendered application;
- routes;
- layouts;
- components;
- shared primitives;
- tokens;
- typography;
- colors;
- spacing;
- responsive behavior;
- motion;
- editor;
- AI;
- JSON Studio;
- terminal;
- Git;
- preview;
- assets;
- themes;
- states;
- build;
- dependencies;
- runtime.

Before changing a feature, determine:

- Does it exist?
- Is it complete?
- Is it visually approved?
- Is it functional?
- Is it connected?
- Is it JSON-driven?
- Is it reusable?
- Is it duplicated?
- Is it production-safe?
- Can it be completed without redesign?

Action rules:

Strong → PRESERVE  
Incomplete → COMPLETE  
Inconsistent → NORMALIZE WITHOUT REDESIGN  
Duplicated → CONSOLIDATE CAREFULLY  
Broken → REPAIR  
Placeholder → REPLACE ONLY THE PLACEHOLDER  
Technically weak but visually approved → REFACTOR INTERNALS, PRESERVE OUTPUT

---

## 27. VISUAL EQUIVALENCE RULE

Refactoring must preserve:

- composition;
- hierarchy;
- typography;
- scale;
- spacing intention;
- alignment;
- depth;
- interaction;
- motion personality;
- responsive intent;
- dark mode;
- light mode.

Do not trade visual quality for implementation convenience.

---

## 28. CURRENT RENDERED PRODUCT AS VISUAL REFERENCE

Rendered reality has priority over outdated documentation.

Current approved implementation has priority over abandoned experiments.

Production-quality work has priority over placeholders.

User-authored work has priority over generated replacement content.

---

## 29. CHANGE CLASSIFICATION RULE

Classify every significant modification as:

PRESERVE  
REPAIR  
COMPLETE  
CONNECT  
CONSOLIDATE  
OPTIMIZE  
REFINE  
REPLACE PLACEHOLDER  
REMOVE VERIFIED DEAD CODE

Do not use REDESIGN unless explicitly requested by the product owner.

Required sequence:

INSPECT → CLASSIFY → PRESERVE → COMPLETE → CONNECT → VALIDATE → POLISH → DOCUMENT

---

## 30. DESIGN REGRESSION PROTECTION

Regression includes:

- loss of asymmetry;
- loss of typography contrast;
- loss of spatial composition;
- generic cards;
- excessive radius, pills, glass, glow, gradients;
- weakened X identity;
- uncontrolled red;
- simplified motion;
- removed feedback;
- broken dark mode;
- broken light mode;
- broken mobile;
- flattened hierarchy;
- generic SaaS or IDE styling;
- placeholder-quality implementation;
- disconnected modules.

Repair regression before completion.

---

## 31. SOURCE-OF-TRUTH PRIORITY

When sources conflict:

1. Latest explicit instruction from the product owner.
2. Current approved rendered implementation.
3. Current production-quality repository implementation.
4. This design-preservation specification.
5. Current tokens and shared primitives.
6. Current JSON and schemas.
7. Current documentation.
8. Historical documentation.
9. Old experiments and abandoned assets.
10. Generic assumptions.

When uncertainty remains, preserve the strongest existing implementation.

---

## 32. FINAL PRESERVATION EXECUTION DIRECTIVE

WHISPERX | STUDIO already has an approved identity.

The task is to make it real.

Inspect before editing.

Preserve before replacing.

Reuse before recreating.

Complete before expanding.

Connect before simulating.

Validate before claiming.

Refine before redesigning.

Production-ready where verified.

Ship the approved product.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PART II — CONTROLLED SPATIAL EXPANSION

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Sections 1–32 remain mandatory.

Spatial expansion may extend the approved visual world but may not redesign, replace, simplify, or obscure the approved interface.

---

## 33. SPATIAL EXPANSION BOUNDARY

Spatial expansion means:

- evolving approved objects;
- strengthening narrative importance;
- connecting objects to product capabilities;
- improving depth, hierarchy, state communication, and continuity;
- extending the same identity across routes.

It does not mean:

- replacing the interface;
- rebuilding the landing page;
- creating a separate 3D product;
- turning the product into a technology showcase;
- covering functional surfaces;
- reducing readability;
- adding motion for spectacle;
- changing Studio layout for decoration.

The interface remains primary.

The spatial world supports it.

---

## 34. WHISPERX SPATIAL WORLD

Create one coherent world where:

ORGANIC × CODE × AI × AGENTS × COMPUTE × EDITORIAL × MOTION

coexist.

It must feel intentional, intelligent, calm, architectural, organic, computational, cinematic, restrained, and alive.

It must not feel like cyberpunk, a particle demo, a 3D portfolio template, a game, or generic AI visualizer.

---

## 35. EXISTING ORGANIC OBJECT PRESERVATION

Preserve existing:

- trees;
- branches;
- leaves;
- botanical forms;
- spheres;
- orbs;
- floating forms;
- geometric objects;
- red X elements;
- spatial compositions.

Meaning:

Trees → creativity, growth, ideas, branching systems.  
Orbs → intelligence, AI state, computation, focus, processing.  
Red X → identity, activation, execution, transformation, threshold.

Prefer EVOLVE APPROVED OBJECT over REPLACE WITH NEW OBJECT.

---

## 36. NEW DEV × AI OBJECT FAMILIES

Approved families:

1. Computational Tree  
2. Intelligent Orb  
3. Code Monolith  
4. Agent Core and Agent Nodes  
5. JSON Slabs  
6. Terminal Planes  
7. Build Artifacts  
8. X Portals  
9. Neural Threads  
10. Micro Code Typography

All technical content must be plausible, relevant, readable where intended, and consistent with the actual stack.

Do not use fake random code, fake terminal output, fake success states, or generic glowing AI objects.

---

## 37. SPATIAL OBJECT REGISTRY AND METADATA

Every significant object must define:

- object ID;
- name;
- family;
- semantic meaning;
- approved routes;
- scenes;
- hierarchy tier;
- role;
- asset source;
- implementation type;
- material;
- theme behavior;
- motion preset;
- interaction behavior;
- system-state mapping;
- responsive behavior;
- mobile fallback;
- reduced-motion fallback;
- static fallback;
- loading strategy;
- performance class;
- accessibility notes;
- technical-content source;
- implementation status.

Performance classes:

LIGHTWEIGHT  
MODERATE  
HEAVY  
ROUTE-ISOLATED

Heavy objects must not load globally without justification.

---

## 38. VISUAL HIERARCHY LOCK

Tier 1 — Identity:

- Computational Tree;
- Intelligent Orb;
- Red X.

Tier 2 — System:

- Code Monolith;
- Agent Core;
- JSON Slab;
- Build Artifact;
- X Portal.

Tier 3 — Environment:

- terminal commands;
- code fragments;
- agent nodes;
- coordinates;
- metadata;
- grid lines;
- neural threads;
- micro typography.

Every scene requires:

- one dominant object or content surface;
- one or two supporting objects;
- restrained environmental detail;
- clear visual path;
- sufficient negative space.

---

## 39. ROUTE AND SCENE MAPPING

Landing Hero:

Computational Tree + Red X, with Intelligent Orb secondary.

Studio Home:

Project and workspace content primary. Spatial decoration restrained.

Studio Workspace:

Editor, canvas, preview, or selected work surface primary.

AI / Agents:

Agent Core or Intelligent Orb primary.

JSON Studio:

JSON content and live preview primary.

Motion Lab:

Responsive geometry and motion preview primary.

Editor:

Code primary. Minimal decoration.

Terminal:

Commands and output primary. No unnecessary decoration.

Git:

Real branch state, changes, diff, and commits primary.

Build / Deploy / Ship:

Real build state and deployment progress primary.

---

## 40. SPATIAL MOTION BEHAVIOR

Objects may respond to scroll, velocity, pointer, hover, focus, drag, route transitions, system state, build state, Git state, validation, deployment, and theme.

Every motion must define:

- trigger;
- duration;
- easing;
- interruption;
- completion;
- reduced-motion behavior;
- off-screen behavior;
- cleanup.

Stillness is part of the composition.

---

## 41. RESPONSIVE SPATIAL BEHAVIOR

Desktop:

richer depth, larger objects, controlled parallax.

Tablet:

fewer layers, lighter motion, touch-safe behavior.

Mobile:

one dominant object, simplified composition, low motion, controlled cropping, no desktop scene scaling.

Reduced-motion:

no parallax, no velocity response, no continuous rotation, no animated blur, static composition with full state clarity.

---

## 42. PERFORMANCE AND ACCESSIBILITY LOCK

Prefer:

- CSS transforms;
- opacity;
- GPU-friendly motion;
- shared motion primitives;
- lazy loading;
- route-level loading;
- responsive assets;
- requestAnimationFrame-safe updates;
- intersection activation;
- paused off-screen behavior;
- static fallbacks;
- progressive enhancement.

Avoid:

- unnecessary WebGL;
- permanent loops;
- excessive particles;
- large uncompressed assets;
- heavy blur;
- global pointer calculations;
- layout-triggering animation;
- globally loaded heavy dependencies.

Accessibility:

- decorative objects stay out of the accessibility tree;
- meaningful objects have accessible equivalents;
- no information through motion alone;
- no information through color alone;
- no flashing;
- visible focus;
- keyboard-safe overlays;
- pointer effects never required.

---

## 43. TECHNICAL CONTENT INTEGRITY AND SAFETY

Technical content must be accurate, plausible, contextually relevant, and consistent with the real system.

Never expose:

- secrets;
- tokens;
- API keys;
- private URLs;
- credentials;
- personal information.

Classify technical content as:

LIVE  
REAL-SYSTEM GENERATED  
REPRESENTATIVE EXAMPLE  
SIMULATED  
PLACEHOLDER  
PLANNED

Never present simulated or planned content as live.

---

## 44. SPATIAL ACCEPTANCE CRITERIA

A spatial scene passes only when:

- identity remains recognizably WHISPERX;
- every object has meaning and role;
- existing approved assets are preserved;
- one dominant element exists;
- content remains readable;
- primary actions remain usable;
- desktop, tablet, mobile, touch, short-height, and reduced-motion behavior are validated;
- motion is purposeful and interruptible;
- performance fallbacks exist;
- accessibility is preserved;
- technical content is honest;
- no design regression exists.

---

## 45. FINAL SPATIAL PRINCIPLE

Trees are not decoration.

Orbs are not decoration.

Code is not decoration.

Agents are not decoration.

JSON is not decoration.

Terminal content is not decoration.

Build states are not decoration.

The X is not decoration.

Preserve the approved UX/UI.

Protect the implementation.

Expand the existing world.

Do not redesign.

Make it more distinctive, spatial, meaningful, alive, and unmistakably WHISPERX.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PART III — IMPLEMENTATION REALITY AND PRODUCTION ENFORCEMENT

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The governing relationship is:

APPROVED DESIGN  
× CURRENT IMPLEMENTATION  
× RENDERED REALITY  
× ACTUAL FUNCTION  
× VERIFIED SYSTEM STATE

These must not contradict one another.

---

## 46. IMPLEMENTATION ENFORCEMENT — SPEC MUST BECOME REALITY

This is not documentation-only.

Where implementation work is requested:

1. Inspect.
2. Classify.
3. Preserve.
4. Repair.
5. Complete.
6. Connect.
7. Validate rendered behavior.
8. Validate responsive behavior.
9. Validate dark and light modes.
10. Validate accessibility.
11. Validate performance.
12. Validate build and runtime.
13. Document only what is true.

Repository = implementation target.  
Rendered product = visual validation target.  
Specification = governing contract.

---

## 47. REALITY STATUS AND CLAIM INTEGRITY

Approved statuses:

IMPLEMENTED  
CONNECTED  
PARTIAL  
SIMULATED  
PLACEHOLDER  
PLANNED  
BLOCKED  
UNSUPPORTED  
DEPRECATED  
REMOVED

Never present:

SIMULATED as CONNECTED.  
PLACEHOLDER as IMPLEMENTED.  
PARTIAL as COMPLETE.  
PLANNED as AVAILABLE.  
BLOCKED as SUCCESSFUL.  
UNSUPPORTED as FUNCTIONAL.

Prefer technical honesty over impressive claims.

---

## 48. FINAL PRODUCTION COMPLETION GATE

Do not declare completion because the UI looks polished, routes render, animations work, documentation exists, placeholders exist, or one build passes.

Production readiness requires verified agreement across:

DESIGN × IMPLEMENTATION × FUNCTION × DATA × STATE × RESPONSIVE × ACCESSIBILITY × PERFORMANCE × BUILD × RUNTIME × DOCUMENTATION

The final standard is:

IS FINISHED WHERE CLAIMED.  
IS CONNECTED WHERE CLAIMED.  
IS VALIDATED WHERE CLAIMED.  
IS PRODUCTION-READY WHERE CLAIMED.

---

## 49. EXECUTION ORDER AND PHASE GATE

Required phases:

1. Repository Discovery
2. Rendered Product Inspection
3. Implementation Inventory
4. Foundation Stabilization
5. Preserve and Complete Approved UX/UI
6. Connect Real System Capabilities
7. Controlled Spatial Expansion
8. System-wide Validation
9. Build and Runtime Validation
10. Regression Review
11. Document Verified Reality

Required operating model:

DISCOVER → INSPECT → INVENTORY → STABILIZE → PRESERVE → COMPLETE → CONNECT → EXPAND → VALIDATE → BUILD → REVIEW → DOCUMENT → SHIP

Do not use:

ASSUME → GENERATE → REPLACE → POLISH → CLAIM COMPLETE

Do not advance when an unresolved earlier issue would make later work destructive, misleading, or unreliable.

Independent safe work may continue when blockers are accurately classified and documented.

---

## 50. REQUIRED FINAL DELIVERABLES AND COMPLETION EVIDENCE

Final delivery must include where applicable:

1. Implementation Summary
2. Changed Files Summary
3. Route and Module Status Matrix
4. Capability Status Matrix
5. Spatial Object Registry Status
6. Validation Evidence
7. Reality Status Disclosure
8. Blockers and External Dependencies
9. Regression Confirmation
10. Precise Final Completion Statement

Allowed final statements:

IMPLEMENTATION COMPLETE FOR VERIFIED SCOPE.  
IMPLEMENTATION PARTIAL — VERIFIED BLOCKERS REMAIN.  
BUILD VERIFIED — RUNTIME VALIDATION PARTIAL.  
UX/UI PRESERVED — FUNCTIONAL CONNECTION PARTIAL.  
SPATIAL EXPANSION IMPLEMENTED — MOBILE FALLBACK VALIDATION REMAINS.  
PRODUCTION-READY FOR VERIFIED SCOPE.  
NOT PRODUCTION-READY — UNRESOLVED VERIFIED ISSUES REMAIN.

Never report PASS without actual verification.

---

## 51. AUTONOMOUS EXECUTION DIRECTIVE — DO NOT STOP AT ANALYSIS

When repository access, execution capability, and required permissions are available, proceed autonomously through the verified sequence.

Autonomy does not override:

- approved scope;
- source-of-truth priority;
- phase gates;
- design preservation;
- security boundaries;
- repository permissions;
- deployment restrictions;
- verified blockers;
- production safety.

Do not stop after inspection, analysis, inventory, audit, recommendations, planning, documentation, or identifying incomplete work.

Continue into implementation when work is:

- within verified scope;
- technically possible;
- safe;
- supported by evidence;
- not blocked by a verified dependency.

Do not repeatedly ask whether to continue.

Do not expand scope.

Do not skip phase gates.

Do not deploy to production without explicit authority.

When uncertainty exists:

1. inspect current implementation;
2. inspect related routes and components;
3. inspect rendered behavior;
4. inspect tokens, JSON, schemas, and documentation;
5. apply source-of-truth priority;
6. preserve strongest existing work;
7. make the smallest safe change;
8. validate;
9. continue.

Ask the product owner only when proceeding would materially risk:

- data loss;
- destruction of user-authored work;
- irreversible repository damage;
- security exposure;
- incorrect external integration;
- unintended production deployment;
- major design regression;
- unsupported architectural commitment;
- scope expansion.

Required behavior:

INSPECT → DECIDE FROM EVIDENCE → IMPLEMENT → VALIDATE → CONTINUE

Not:

INSPECT → WRITE REPORT → ASK WHETHER TO CONTINUE

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PART IV — FULL FRONTEND COMPLETION AND SYSTEM CONVERGENCE

ALL PAGES × ALL STATES × JSON × SKILLS × PACKAGES × VISUAL QA

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Sections 1–51 remain mandatory.

This section closes the remaining execution gaps.

Its purpose is to ensure that WHISPERX | STUDIO does not stop at:

* repository analysis;

* partial route implementation;

* documentation;

* shell creation;

* isolated components;

* disconnected tools;

* placeholder pages;

* generic fallback UI;

* visually polished but incomplete frontend surfaces.

The immediate objective is:

COMPLETE THE ENTIRE FRONTEND EXPERIENCE.

Every required page.

Every major route.

Every meaningful state.

Every supported viewport.

Dark mode.

Light mode.

Desktop.

Tablet.

Mobile.

The final frontend must feel like one complete product.

Not a collection of unfinished experiments.

Not a shell waiting for future design.

Not a dashboard with placeholder cards.

Not a set of disconnected technical tools.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

52. COMPLETE FRONTEND DIRECTIVE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Complete all frontend routes required by the actual product architecture.

Do not stop after completing:

* Landing;

* Studio Home;

* Editor;

* one dashboard;

* one workspace;

* one preview screen.

Inspect the current route architecture first.

Preserve all strong existing routes.

Complete all incomplete routes within the verified frontend scope.

The expected frontend architecture may include, where supported by repository evidence:

PUBLIC

* Landing;

* Product;

* Studio Overview;

* Features;

* Modules;

* Skills;

* Design System;

* Motion;

* Documentation;

* Changelog;

* About;

* Sign In;

* Sign Up;

* Forgot Password;

* Reset Password.

APPLICATION

* Home;

* Projects;

* Recent;

* Favorites;

* Templates;

* New Project;

* Import Project;

* Project Overview;

* Project Activity;

* Global Search;

* Notifications;

* Account;

* Settings.

STUDIO

* Studio Home;

* Explorer;

* Editor;

* Visual Canvas;

* Live Preview;

* Responsive Preview;

* AI Chat;

* Agent Workspace;

* Terminal;

* Git;

* Command Palette;

* Problems;

* Output;

* Logs;

* Components;

* Component Inspector;

* Design Tokens;

* Motion Lab;

* JSON Studio;

* JSON Inspector;

* Modules;

* Skills;

* Assets;

* Dependencies;

* Environment;

* Visual QA;

* Build;

* Deploy;

* Project Settings.

Do not blindly create routes merely because they appear in this list.

First inspect whether the capability:

* exists;

* is required;

* is already represented elsewhere;

* should be a route;

* should be a panel;

* should be a modal;

* should be a sheet;

* should be a command surface;

* should be a contextual inspector.

Avoid unnecessary route proliferation.

The product architecture determines the correct surface.

Every implemented frontend surface must have:

* a clear purpose;

* meaningful content;

* appropriate hierarchy;

* approved WHISPERX design language;

* responsive behavior;

* dark-mode behavior;

* light-mode behavior;

* loading state where applicable;

* empty state where applicable;

* error state where applicable;

* interaction states;

* accessibility behavior.

No major frontend route may remain placeholder-only

when it can be safely completed with representative product structure.

Representative product structure must never be misclassified as live data.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

53. FRONTEND COMPLETENESS MATRIX

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create and maintain:

docs/FRONTEND_COMPLETENESS_MATRIX.md

For every route or major work surface record:

* route ID;

* route path;

* surface name;

* purpose;

* desktop status;

* tablet status;

* mobile status;

* dark-mode status;

* light-mode status;

* loading state;

* empty state;

* error state;

* keyboard behavior;

* touch behavior;

* reduced-motion behavior;

* data source;

* JSON source where applicable;

* implementation status;

* validation status;

* unresolved issue;

* required action.

Use only verified statuses.

The matrix is not a substitute for implementation.

Its purpose is to prevent forgotten routes and hidden incomplete surfaces.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

54. NO GENERIC FALLBACK RULE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When completing a missing route:

Do not fall back to:

* generic dashboard;

* generic card grid;

* generic settings page;

* generic SaaS table;

* generic hero;

* generic AI chat;

* generic IDE layout;

* generic empty state;

* generic shadcn composition.

Every new surface must inherit from:

* current WHISPERX composition;

* current design tokens;

* current typography;

* current red signal system;

* current X identity;

* current spatial logic;

* current motion language;

* current density model;

* current responsive philosophy.

If no exact existing route exists:

derive the new surface from the closest approved WHISPERX implementation.

Do not derive it from generic external templates.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

55. JSON MUST DRIVE REAL SYSTEM BEHAVIOR

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do not treat JSON as passive documentation.

Inspect all existing JSON.

Search the repository for:

* JSON files;

* JSON imports;

* JSON parsing;

* schemas;

* token files;

* content configuration;

* route configuration;

* motion configuration;

* component definitions;

* skills metadata;

* module metadata;

* workspace state;

* project metadata;

* preview configuration.

For each significant JSON source determine:

* what it represents;

* whether it is valid;

* whether it is current;

* whether it is duplicated;

* whether it is actually consumed;

* whether it should drive runtime behavior;

* whether it needs a schema;

* whether it needs migration;

* whether it needs versioning.

Important JSON must have a real consumption path where appropriate:

JSON SOURCE

→ VALIDATION

→ TYPE

→ TRANSFORMATION

→ RUNTIME CONSUMPTION

→ UI

→ ERROR HANDLING

→ VERSIONING

Do not leave important JSON disconnected.

Do not hard-code values already correctly defined in an approved JSON source.

Do not force all product behavior into JSON when code is more appropriate.

Use JSON where it creates real value.

Create or update:

docs/JSON_RUNTIME.md

docs/JSON_SCHEMA_GUIDE.md

docs/JSON_MIGRATION.md

docs/JSON_CONSUMPTION_MAP.md

The JSON Consumption Map must identify:

* JSON source;

* owner;

* schema;

* consumer;

* runtime role;

* status;

* fallback;

* migration requirement.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

56. GLOBAL DESIGN SPEC ENFORCEMENT

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If present, inspect:

global-tw4-master.v7.full-spec.json

Do not assume it is automatically authoritative.

Compare it against:

* current rendered product;

* current design tokens;

* Tailwind configuration;

* CSS variables;

* theme implementation;

* component implementation;

* motion primitives.

Where it matches approved current implementation:

preserve and integrate it.

Where it is stale:

do not allow it to overwrite newer approved work.

Where it duplicates existing tokens:

consolidate carefully.

Where it contains valuable unused definitions:

evaluate whether they should be connected.

Where it conflicts with rendered approved reality:

rendered approved reality has priority.

Document the decision.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

57. PACKAGE DISCOVERY AND IMPLEMENTATION RULE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do not rebuild mature infrastructure unnecessarily.

Before implementing difficult technical capabilities, inspect:

* existing dependencies;

* existing packages;

* existing internal utilities;

* existing workspace packages;

* existing lockfile;

* package compatibility;

* framework version.

Evaluate mature packages where appropriate for:

* editor engine;

* terminal emulation;

* resizable panels;

* keyboard shortcuts;

* command palette;

* diff rendering;

* syntax highlighting;

* JSON schema validation;

* code validation;

* virtualized lists;

* drag and drop;

* animation;

* Git visualization;

* state management;

* accessibility primitives;

* testing;

* visual regression.

Do not install a package simply because it is popular.

Before adding a dependency verify:

1. Is it necessary?

2. Does the repository already solve this?

3. Is it maintained?

4. Is it compatible?

5. What is its bundle cost?

6. What is its runtime cost?

7. What is its license?

8. Does it create security risk?

9. Does it duplicate another package?

10. Can it preserve the approved WHISPERX design?

Do not allow package defaults to visually redesign the product.

Third-party packages are implementation infrastructure.

WHISPERX design remains authoritative.

Document major decisions in:

docs/TECHNOLOGY_DECISIONS.md

docs/PACKAGE_AUDIT.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

58. SKILLS DISCOVERY AND SKILLS REGISTRY

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Use available skills sources as capability references where technically available.

Do not install everything blindly.

Search for useful skills related to:

* Next.js;

* React;

* TypeScript;

* Tailwind CSS;

* Framer Motion;

* HTML;

* CSS;

* JavaScript;

* frontend architecture;

* responsive design;

* accessibility;

* animation;

* motion;

* visual QA;

* code review;

* testing;

* Git;

* terminal workflows;

* JSON;

* schemas;

* documentation;

* deployment;

* security;

* performance.

For each candidate skill inspect:

* identity;

* source;

* version;

* capability;

* compatibility;

* maintenance;

* security implications;

* dependencies;

* overlap with existing capabilities.

Build a real Skills Registry where supported.

Each skill should expose metadata such as:

* id;

* name;

* description;

* category;

* source;

* version;

* status;

* capabilities;

* compatibility;

* dependencies;

* permissions;

* installation state;

* enabled state.

Do not classify a skill as installed without evidence.

Do not classify a skill as functional without execution evidence.

Build appropriate WHISPERX surfaces for:

* Skills Library;

* Installed Skills;

* Skill Detail;

* Search;

* Filter;

* Enable or Disable;

* Compatibility;

* Dependencies;

* Capability Status.

The Skills UI must not resemble an app-store card grid by default.

Use the approved WHISPERX spatial and editorial language.

Create or update:

docs/SKILLS_SYSTEM.md

docs/SKILLS_AUDIT.md

docs/SKILLS_REGISTRY.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

59. MODULE CONVERGENCE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The product must not feel like disconnected applications.

Editor.

Preview.

Terminal.

Git.

AI Chat.

Agents.

JSON Studio.

Motion Lab.

Components.

Skills.

Modules.

Visual QA.

Build.

Deploy.

These must share:

* project context;

* workspace context;

* selection context where applicable;

* theme;

* navigation logic;

* command system;

* status language;

* visual identity;

* capability honesty.

Where appropriate, actions in one surface should affect another.

Examples:

selected file

→ editor

→ AI context

→ preview

selected component

→ inspector

→ JSON

→ motion

→ preview

agent task

→ proposed files

→ diff

→ approval

→ editor

→ terminal

→ validation

JSON change

→ schema validation

→ runtime update

→ preview state

build command

→ terminal

→ output

→ problems

→ build state

Do not fake these relationships.

Connect them where technically possible.

Classify honestly where they remain simulated or partial.

Create:

docs/SYSTEM_CONTEXT_MODEL.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

60. VISUAL QA AND RENDERED REGRESSION LOOP

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Source-code inspection alone is insufficient for visual work.

Where rendered execution is technically available, use a visual validation loop:

INSPECT CURRENT RENDER

→ IMPLEMENT

→ RENDER

→ COMPARE

→ IDENTIFY REGRESSION

→ REPAIR

→ VALIDATE AGAIN

Validate significant routes at representative viewport sizes.

At minimum where applicable:

DESKTOP

* large desktop;

* standard desktop;

* laptop.

TABLET

* landscape;

* portrait.

MOBILE

* common portrait;

* narrow portrait;

* landscape where applicable.

Validate:

* dark mode;

* light mode;

* loading;

* empty;

* error;

* focus;

* hover;

* active;

* disabled;

* reduced motion.

Protect against:

* accidental cardification;

* flattened hierarchy;

* broken asymmetry;

* incorrect typography;

* excessive radius;

* uncontrolled red;

* lost X identity;

* clipping;

* overflow;

* unreadable code;

* overlapping controls;

* broken mobile layout;

* excessive object density;

* spatial objects blocking content.

Where automated screenshot or visual-regression tooling already exists:

use it.

Where it does not exist and introducing it is justified:

evaluate a mature solution.

Do not add heavy visual infrastructure without clear value.

Create:

docs/VISUAL_QA.md

docs/VISUAL_REGRESSION_REPORT.md

Do not claim visual validation if rendered output was not actually inspected.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

61. FRONTEND DATA HONESTY

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A complete frontend may require representative content before real backend integration exists.

This is allowed only when classified honestly.

Representative frontend data must be identified as:

* REPRESENTATIVE;

* SIMULATED;

* PLACEHOLDER;

where applicable.

Do not leave a route visually empty merely because a backend is unavailable

if meaningful representative product structure can be safely created.

However:

do not present representative content as live.

Do not fake:

* connected Git state;

* successful terminal commands;

* live AI execution;

* successful deployment;

* real build completion;

* repository changes;

* user data;

* external integrations.

The frontend may demonstrate intended UX.

It must not misrepresent system reality.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

62. NO PLACEHOLDER-QUALITY PAGE RULE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A route is not complete when it contains only:

* page title;

* lorem ipsum;

* one generic card;

* one empty table;

* one decorative mockup;

* one coming-soon message;

* disconnected buttons;

* fake metrics.

Every required frontend route must provide enough designed structure

to communicate its actual purpose.

If functionality is unavailable:

design the honest state.

Use:

* planned state;

* unsupported state;

* blocked state;

* representative state;

* setup-required state;

* connection-required state.

Do not hide incompleteness.

Design incompleteness professionally.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

63. DESIGN TOKEN CONVERGENCE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit all visual constants.

Search for duplicated:

* colors;

* red values;

* spacing;

* radius;

* shadows;

* blur;

* typography;

* z-index;

* animation duration;

* easing;

* spring configuration;

* breakpoints.

Consolidate where safe.

Do not over-normalize intentional asymmetry.

The goal is not to make every value identical.

The goal is to distinguish:

SYSTEM TOKEN

from:

INTENTIONAL COMPOSITION VALUE

Do not destroy editorial composition by forcing every spacing value into one rigid scale.

Do not destroy spatial hierarchy by giving every panel the same radius.

Create:

docs/TOKEN_AUDIT.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

64. MOTION IMPLEMENTATION ENFORCEMENT

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit all motion implementation.

Identify:

* Framer Motion usage;

* CSS animation;

* transitions;

* requestAnimationFrame;

* pointer listeners;

* scroll listeners;

* Intersection Observer;

* GSAP where present;

* WebGL where present;

* canvas animation where present.

Classify each major motion system as:

* PRESERVE;

* REPAIR;

* CONSOLIDATE;

* OPTIMIZE;

* REMOVE VERIFIED DEAD CODE.

Do not replace strong custom motion with generic fade-up animations.

Do not add animation merely because a page appears static.

Motion must support:

* state;

* hierarchy;

* continuity;

* interaction;

* spatial relationship.

Create:

docs/MOTION_AUDIT.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

65. MOBILE COMPLETION GATE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Mobile must not be treated as a final responsive cleanup pass.

For every major route determine:

* primary mobile task;

* primary mobile content;

* navigation method;

* panel replacement behavior;

* inspector behavior;

* AI behavior;

* preview behavior;

* terminal availability;

* Git availability;

* touch behavior;

* keyboard behavior where relevant;

* object fallback;

* reduced-motion behavior.

Do not simply hide difficult desktop functionality.

Provide an intentional mobile alternative.

Examples:

multi-panel desktop

→ focused mobile mode switcher

persistent inspector

→ contextual sheet

activity rail

→ bottom dock or command surface

desktop preview controls

→ compact device selector

AI side panel

→ full-height sheet or dedicated work surface

Do not reproduce unreadably dense desktop IDE layouts on mobile.

Create:

docs/MOBILE_COMPLETION_MATRIX.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

66. FINAL FRONTEND COMPLETION GATE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do not declare frontend completion until the verified scope satisfies:

* major routes are implemented;

* major workflows are represented;

* no required route remains placeholder-quality without honest classification;

* dark mode is complete;

* light mode is complete;

* desktop is validated;

* tablet is validated;

* mobile is intentionally designed;

* loading states exist where required;

* empty states exist where required;

* error states exist where required;

* interaction states exist;

* keyboard behavior is addressed;

* touch behavior is addressed;

* reduced motion is addressed;

* JSON is consumed where appropriate;

* skills are honestly represented;

* modules are connected conceptually and technically where possible;

* design tokens are coherent;

* motion is coherent;

* spatial objects are controlled;

* technical content is honest;

* visual regressions are reviewed;

* production build is validated where possible.

The frontend is not complete because every route technically renders.

The frontend is complete when the product feels coherent,

intentional, responsive, usable, and unmistakably WHISPERX.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

67. FINAL AUTONOMOUS FRONTEND EXECUTION DIRECTIVE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do not stop after creating documentation.

Do not stop after auditing routes.

Do not stop after building the Studio shell.

Do not stop after one polished screen.

Do not repeatedly ask for confirmation.

Inspect the repository.

Inspect the rendered product.

Determine verified scope.

Preserve approved work.

Complete every safely executable frontend surface within that scope.

Connect real systems where technically possible.

Use JSON where appropriate.

Use mature packages where justified.

Use skills where verified and valuable.

Complete desktop.

Complete tablet.

Complete mobile.

Complete dark mode.

Complete light mode.

Complete states.

Validate rendered reality.

Protect design.

Protect motion.

Protect spatial composition.

Protect the X.

Do not redesign WHISPERX.

Execute this specification against the current repository. Do not summarize it. Do not create a new design concept. Inspect the repository and rendered product first, determine the verified frontend scope from repository evidence, then autonomously implement all safely executable in-scope work through the required phase sequence. Preserve the approved WHISPERX | STUDIO identity and existing production-quality implementation. Do not stop at analysis, audit, planning, documentation, shell creation, or partial route completion. Continue until the verified scope is complete or a verified technical blocker prevents further execution. Validate every claim and finish with the required completion evidence defined in Section 50.

Do not treat token limits, context length, task size, number of files, number of routes, or implementation duration as reasons to intentionally reduce the verified scope. Work incrementally, preserve progress, maintain implementation records, and continue through the verified execution sequence for as long as execution capability remains available. If execution is interrupted by an actual platform, tool, permission, or environment limitation, record the exact verified stopping point, preserve completed work, classify remaining work accurately, and provide a continuation-ready handoff.

Finish WHISPERX.

The required final state is:

SAME APPROVED PRODUCT.

COMPLETE FRONTEND.

CONNECTED SYSTEM WHERE VERIFIED.

HONEST STATUS WHERE NOT CONNECTED.

NO GENERIC SAAS FALLBACK.

NO GENERIC IDE FALLBACK.

NO PLACEHOLDER-QUALITY ROUTES.

NO FALSE CAPABILITY CLAIMS.

PRODUCTION-POLISHED.

PRODUCTION-VALIDATED WHERE CLAIMED.

SHIP ONLY VERIFIED WORK.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
