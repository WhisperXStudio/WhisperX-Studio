import Link from "next/link";
import { notFound } from "next/navigation";
import { StatusPill } from "@/components/studio/status-pill";
import { getLibraryItem, getRelatedSkills } from "@/lib/studio-registry";
export default async function LibraryDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const item = getLibraryItem(id); if (!item) notFound(); const skills = getRelatedSkills(item); return <main className="min-h-screen px-6 py-10 lg:px-12"><Link href="/library" className="font-mono text-xs uppercase tracking-[0.3em] text-muted-foreground">Library / {item.id}</Link><section className="mt-10 grid gap-8 lg:grid-cols-[1.2fr_0.8fr]"><div><StatusPill status={item.status as any}/><h1 className="mt-8 font-display text-7xl leading-none">{item.title}</h1><p className="mt-8 max-w-2xl text-xl text-muted-foreground">{item.summary}</p><div className="mt-8 flex flex-wrap gap-2">{item.tags.map((tag) => <span key={tag} className="border border-border px-3 py-1 font-mono text-xs uppercase tracking-[0.18em]">{tag}</span>)}</div></div><aside className="border border-border p-6"><h2 className="font-display text-4xl">Related skills</h2>{skills.map((skill) => <div key={skill.id} className="mt-5 border-t border-border pt-5"><StatusPill status={skill.status as any}/><p className="mt-3 font-medium">{skill.name}</p><p className="mt-2 text-sm text-muted-foreground">{skill.description}</p></div>)}</aside></section></main>; }
