import Link from "next/link";
import { StatusPill } from "@/components/studio/status-pill";
import { getRegistry } from "@/lib/studio-registry";
export default function LibraryPage() { const { library } = getRegistry(); return <main className="min-h-screen px-6 py-10 lg:px-12"><Link href="/studio" className="font-mono text-xs uppercase tracking-[0.3em] text-muted-foreground">WHISPERX | STUDIO / Library</Link><h1 className="my-10 font-display text-7xl">Library index</h1><div className="grid gap-4 md:grid-cols-3">{library.map((item) => <Link key={item.id} href={`/library/${item.id}`} className="border border-border p-6 hover:border-red-500/50"><div className="flex justify-between gap-4"><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">{item.collection}</p><StatusPill status={item.status as any}/></div><h2 className="mt-12 font-display text-4xl">{item.title}</h2><p className="mt-4 text-sm text-muted-foreground">{item.summary}</p></Link>)}</div></main>; }
