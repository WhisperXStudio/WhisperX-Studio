import { getRegistry } from "@/lib/studio-registry"; import { StatusPill } from "@/components/studio/status-pill";
export default function ImportPage(){const {imports}=getRegistry(); return <main className="min-h-screen px-6 py-10 lg:px-12"><p className="font-mono text-xs uppercase tracking-[0.3em] text-muted-foreground">WHISPERX import</p><h1 className="my-10 font-display text-7xl">Import lifecycle</h1><div className="space-y-4">{imports.map((entry)=><section key={entry.source} className="border border-border p-6"><div className="flex justify-between"><h2 className="font-display text-4xl">{entry.source}</h2><StatusPill status={entry.status as any}/></div><p className="mt-6 font-mono text-xs text-muted-foreground">{entry.steps.length ? entry.steps.join(" → ") : "UNSUPPORTED"}</p></section>)}</div></main>}
